{
  "name": "used-car-advisor-frontend",
  "private": true,
  "description": "Source for the pre-built browser client in src/used_car_advisor/static/ — maintainers only, see README.md",
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --minify --jsx=automatic --target=es2020 --define:process.env.NODE_ENV=\\\"production\\\" --outfile=dist/app.js"
  },
  "dependencies": {
    "@livekit/components-react": "^2.9.23",
    "@livekit/components-styles": "^1.2.0",
    "livekit-client": "^2.21.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
